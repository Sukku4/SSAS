#include "utils/data_logger.h"

void DataLogger::start() {
    // Initialize logging mechanism (e.g., SD card, Serial)
}

void DataLogger::logData(const std::map<String, float>& data) {
    // Log data to the chosen medium
}
